{
  "name": "ledgex-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Ledgex group sharing.",
  "scripts": {
    "lint": "eslint --ext .ts src",
    "build": "tsc",
    "deploy": "firebase deploy --only functions:createTripInvite",
    "serve": "firebase emulators:start --only functions"
  },
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^11.10.1",
    "firebase-functions": "^6.5.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "eslint": "^8.57.0",
    "eslint-config-google": "^0.14.0",
    "firebase-functions-test": "^2.4.0",
    "typescript": "^5.4.5"
  }
}
